Linux only. At the moment only the python code is relevant.

*.mat file format for graph markov chains:

N = n of markov chain

A = adjacency matrix of graph

P = transposed transition matrix of the markox chain

x_t = matrix with any number of columns where each column is the distibution after time t and t=0 <=> inital distibution

stationary = stationary distribution
